machine:
    services:
        - docker

dependencies:
    override:
        - sudo pip install --upgrade docker-compose==1.9.0

test:
    override:
        - docker-compose run -d --no-deps node1
        - cd node; mocha